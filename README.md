# Venite University Redesign

Built with React, Vite, GSAP, and custom CSS variables.
Features a premium dark aesthetic with advanced scroll animations.

- Routing via `react-router-dom`
- Animations via `gsap`
- Deploys on Vercel (`vercel.json` included)
